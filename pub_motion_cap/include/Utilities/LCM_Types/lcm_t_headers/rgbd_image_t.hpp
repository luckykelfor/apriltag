/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <lcm/lcm_coretypes.h>

#ifndef __lcm_t_headers_rgbd_image_t_hpp__
#define __lcm_t_headers_rgbd_image_t_hpp__

#include <vector>

namespace lcm_t_headers
{

class rgbd_image_t
{
    public:
        int64_t    timestamp;
        int32_t    rgb_size;
        std::vector< uint8_t > rgb_data;
        int32_t    d_size;
        std::vector< uint8_t > d_data;

    public:
        inline int encode(void *buf, int offset, int maxlen) const;
        inline int getEncodedSize() const;
        inline int decode(const void *buf, int offset, int maxlen);
        inline static int64_t getHash();
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static int64_t _computeHash(const __lcm_hash_ptr *p);
};

int rgbd_image_t::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int rgbd_image_t::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int rgbd_image_t::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t rgbd_image_t::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* rgbd_image_t::getTypeName()
{
    return "rgbd_image_t";
}

int rgbd_image_t::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->timestamp, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->rgb_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __byte_encode_array(buf, offset + pos, maxlen - pos, &this->rgb_data[0], this->rgb_size);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->d_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __byte_encode_array(buf, offset + pos, maxlen - pos, &this->d_data[0], this->d_size);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int rgbd_image_t::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->timestamp, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->rgb_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    this->rgb_data.resize(this->rgb_size);
    if(this->rgb_size) {
        tlen = __byte_decode_array(buf, offset + pos, maxlen - pos, &this->rgb_data[0], this->rgb_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->d_size, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    this->d_data.resize(this->d_size);
    if(this->d_size) {
        tlen = __byte_decode_array(buf, offset + pos, maxlen - pos, &this->d_data[0], this->d_size);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int rgbd_image_t::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int64_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __byte_encoded_array_size(NULL, this->rgb_size);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __byte_encoded_array_size(NULL, this->d_size);
    return enc_size;
}

int64_t rgbd_image_t::_computeHash(const __lcm_hash_ptr *)
{
    int64_t hash = 0xc060260cbe330ebeLL;
    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
